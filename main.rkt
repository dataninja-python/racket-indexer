#lang racket


(define (init-game)
    ;; this function does x
    displayln "Hi from the init-game function"
)

;; @problem:
;; @solution:
;; @input:
;; @output:
;; @details: main runs the guessing game
(define (main)
    (init-game)
)

(main)